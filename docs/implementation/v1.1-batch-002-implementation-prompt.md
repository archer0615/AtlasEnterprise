# v1.1 BATCH-002 Implementation Prompt

## Objective

Implement v1.1 BATCH-002 with only the approved scope:

- CAND-004: Notification duplicate prevention.
- CAND-005: Backup coverage plan for contract-only stores.

## Constraints

- Do not add domains, business concepts, formulas, state-machine states, IndexedDB migrations, service worker changes, backend, cloud runtime, remote database, external analytics, AI, or automated financial execution.
- Keep `knowledge/**/*.md` as the only canonical source when knowledge changes are required.
- Keep runtime local-first, static-first, and offline-first.
- Keep contract-only stores as non-persistent until migration, backup, restore, owner-index, audit, and validation evidence are complete.
- Mark unsupported runtime surfaces as `Not Implemented` instead of implying hidden backend or cloud behavior.

## Candidate Scope

| Candidate | Scope | Expected Outcome |
| --- | --- | --- |
| CAND-004 | Notification duplicate prevention | Duplicate local notification records are not generated for the same owner, source, due date, and notification category. |
| CAND-005 | Backup coverage plan for contract-only stores | Documentation and validation plan identify backup expectations before persistence is implemented. |

## Required Investigation

1. Review existing notification runtime contracts and tests.
2. Identify the stable dedupe key fields already present in local notification records.
3. Confirm CAND-005 remains documentation/test-plan only and does not add persistence or migrations.
4. Review backup, restore, and audit status docs for current implemented stores.
5. Confirm no rejected, deferred, needs-specification, or needs-architecture-decision candidate is pulled into active scope.

## Implementation Guidance

1. Add duplicate prevention in the notification generation boundary using existing fields only.
2. Prefer deterministic key construction from structured fields over ad hoc message text matching.
3. Keep generated notification read state behavior unchanged.
4. Add or update focused tests proving duplicate prevention and preserving existing notification output.
5. Add backup coverage planning documentation for contract-only stores including recommendations, execution plans, action plans, notifications, calendar entries, automation rules, and scheduler state.
6. Update roadmap/status documents only where they reflect BATCH-002 progress.

## Expected File Areas

| Path | Change Type | Reason |
| --- | --- | --- |
| `frontend/src/features/notifications/` | Modify | Notification duplicate prevention. |
| `frontend/scripts/test-notification-automation-*.mjs` | Modify | Focused notification dedupe validation. |
| `docs/roadmap/` | Modify/Add | Backup coverage plan and roadmap status alignment. |
| `.codex/atlas-v1.1-implementation-queue.json` | Modify | Mark BATCH-002 progress only after validation evidence exists. |
| `docs/status/next-priority.md` | Modify | Advance priority only after BATCH-002 completion. |

## Validation Required

```powershell
npm run validate:quick
npm run validate:full
```

## Completion Criteria

- CAND-004 duplicate prevention has test evidence.
- CAND-005 backup coverage plan is documented without adding persistence.
- `npm run validate:quick` passes.
- `npm run validate:full` passes.
- BATCH-002 status is updated only after validation passes.
