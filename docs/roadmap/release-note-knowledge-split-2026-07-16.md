# Release Note - Knowledge Split 2026-07-16

## Scope

Completed the current knowledge split and documentation governance batch across entity, reporting, framework, security, and legacy reference documents.

## Highlights

- Added split navigation and child review documents for Goal, ActionPlan, ExecutionPlan, Recommendation, Decision Reporting, Goal Reporting, Scenario Framework, Goal Execution, Goal Metrics, Goal Dashboard, and Security Architecture.
- Marked legacy entity documents under `docs/knowledge/entity/**` as reference-only and linked each one to its canonical `knowledge/entity/**` source.
- Added roadmap audit artifacts for large documents, duplication, link checking, consistency templates, legacy retirement, and worker progress reports.
- Used sub agents for Worker AI, Worker AJ, and Worker AK.
- Rebuilt generated frontend knowledge artifacts.

## Validation

- `npm run build:knowledge` generated 608 knowledge documents.
- `npm run validate` passed frontend, fixtures, PWA, offline cache, and simulator validation.
- Remaining high-line-count files without split navigation or legacy marking: 0.

## Git Notes

- No commit was created.
- No remote push was attempted.
- `frontend/knowledge/**` changes are generated by `npm run build:knowledge`.
