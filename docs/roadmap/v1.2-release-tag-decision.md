# v1.2 Release Tag Decision

## Current State

- Release tag: `v1.2.0`
- Tag target commit: `aeb72c4 Record v1.2 validation evidence`
- Current HEAD: `49ec370 Start CSV dry-run parser runtime batch`

## Decision

Do not rebuild or force-move `v1.2.0` automatically.

## Reason

`v1.2.0` has already been pushed to the remote repository. Moving a published release tag changes the release point and should require explicit operator approval.

## Follow-Up

If the CSV dry-run parser should be part of a tagged release, create a new tag such as `v1.2.1` or explicitly approve moving `v1.2.0`.
