# v1.2.2 GitHub Release Draft

## Tag

`v1.2.2`

## Target

`b6b466d Record post-tag release follow-up evidence`

## Title

Atlas Enterprise v1.2.2

## Summary

v1.2.2 closes the local release evidence package for insurance CSV import, insurance runtime validation, backup restore UX coverage, position reporting status, release dry-run evidence, visual baseline refresh, archive readiness, and post-archive monitoring.

## Highlights

- Insurance CSV import mapping, commit path, update/cancel actions, owner isolation, duplicate-name feedback, restore conflict policy, coverage summary, and audit history were validated.
- Position reporting UI status and persistence gates were closed for read-only reporting readiness.
- CSV write path expansion was archived with validated local entity coverage.
- Backup schema v2 fixtures, restore UX, compatibility, and security gates passed.
- Release, archive, maintenance, handoff, retirement, and final archive audit validations passed.
- GitHub Pages public site and workflow evidence were confirmed after `v1.2.2` tag and v1.3 planning commits.

## Validation Evidence

- `npm run validate:quick` passed with 60/60 steps.
- `npm run validate:feature` passed with 69/69 steps.
- `npm run validate:full` passed with 81/81 steps.
- `npm run validate:release:dry-run` passed with 103/103 steps.
- `npm run validate:release` passed with 103/103 steps.
- GitHub Pages site returned `200 OK`.
- GitHub Actions deploy workflow run `30510773373` completed with conclusion `success`.
- GitHub Pages build deployment workflow run `30510772910` completed with conclusion `success`.

## Known Constraints

- GitHub Release page creation remains blocked without `GH_TOKEN`, `GITHUB_TOKEN`, `gh` CLI, or manual operator action.
- No backend, cloud database, authentication server, sync service, external analytics, AI runtime, remote notification, market data, or automated financial execution is introduced.

