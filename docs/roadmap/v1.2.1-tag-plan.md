# v1.2.1 Tag Plan

## Scope

`v1.2.1` should tag the validation work added after `v1.2.0`.

## Candidate Included Commits

- `49ec370 Start CSV dry-run parser runtime batch`
- `ca6b5e7 Add CSV fixtures and position contract planning`
- `8e2cb14 Add CSV dry-run UI validation gates`

## Tag Rule

Create `v1.2.1` only after:

- GitHub Release for `v1.2.0` is either published or explicitly skipped.
- `npm run validate:full` passes on a clean working tree.
- GitHub Actions and Pages deployment pass for the selected commit.

## Release Note

`v1.2.1` is a validation/UI gate release for CSV dry-run preview, Position repository contract gates, Position IndexedDB migration gates, and Backup schema v2 fixture planning.
