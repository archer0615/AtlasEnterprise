# v1.2 Next Runtime Batch Selection

## Decision

The next runtime batch remains deferred.

## Reason

The validation gate batch is complete for Position persistence, backup schema compatibility, and CSV import security. Runtime implementation still requires a separately selected batch with explicit migration, rollback, UI, and user-data safety scope.

## Eligible Runtime Candidates

| Candidate | Current Gate | Runtime Selection |
| --- | --- | --- |
| Position persistence | ADR and validation gate complete | Eligible for a dedicated persistence batch after migration and rollback plan confirmation |
| Backup schema versioning | ADR and compatibility gate complete | Eligible only when a payload-changing feature is selected |
| CSV import | Specification and security gate complete | Dry-run parser started; write-path implementation remains blocked |

## Not Selected

- Position IndexedDB store.
- Backup payload schema mutation.
- CSV import write path.

## Required Before Runtime Work

- Dedicated implementation prompt.
- Migration and rollback evidence.
- Backup/restore acceptance evidence.
- Browser-visible error reporting evidence.
- Release profile passing on a clean working tree.

## Started

- CSV dry-run parser domain module.
- Position migration plan.
