# v1.3 CAND-301 Release Operator Closure Package

## Scope

CAND-301 remains the only v1.3 release evidence blocker that cannot be closed from this environment.

## Evidence Collected

| Item | Result | Evidence |
| --- | --- | --- |
| Local working tree | Passed | `git status --short` returned clean before this package was created. |
| GitHub token | Blocked | `GH_TOKEN` and `GITHUB_TOKEN` are not set. |
| GitHub CLI | Blocked | `gh` command is unavailable. |
| GitHub Release page | Missing | GitHub API for `releases/tags/v1.2.2` returned 404. |
| Remote tag | Passed | `refs/tags/v1.2.2` exists on `origin`. |
| Release draft | Ready | `docs/roadmap/v1.2.2-github-release-draft.md`. |

## Manual Operator Steps

1. Open `https://github.com/archer0615/AtlasEnterprise/releases/new`.
2. Select tag `v1.2.2`.
3. Use title `Atlas Enterprise v1.2.2`.
4. Use the body from `docs/roadmap/v1.2.2-github-release-draft.md`.
5. Publish the release.
6. Re-run GitHub Release API evidence check.

## Closure Rule

CAND-301 can close only after the GitHub Release page exists or the operator explicitly records that GitHub Release publication is deferred.

