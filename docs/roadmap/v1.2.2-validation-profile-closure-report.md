# v1.2.2 Validation Profile Closure Report

## Scope

- v1.2.2 validation profile closure
- release evidence update
- preview smoke validation

## Changes

- Added `test:insurance-runtime-validation` as the executable insurance runtime validation aggregate.
- Added `insurance-runtime-validation` to quick, feature, full, and release validation profiles.
- Updated validation profile status documentation to include insurance runtime validation evidence.
- Fixed PWA runtime resilience test process cleanup so validation profiles can continue after the test passes.
- Moved insurance UI repository access behind composition platform injection to preserve runtime boundaries.

## Validation

- `npm run test:insurance-runtime-validation` passed.
- `npm run validate:profiles` passed.
- `npm run validate:preview-smoke` passed.
- `npm run test:pwa-runtime-resilience` passed.
- `npm run test:runtime-boundary-cleanup` passed.
- `npm run validate:runtime-boundaries` passed.
- `npm run validate:quick` passed with 60/60 steps.

## Notes

- `npm run validate:release:dry-run` was attempted and passed early gates through application bootstrap before the outer shell timeout. Full release dry-run remains a follow-up long-running validation item.
