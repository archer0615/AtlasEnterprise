# v1.2.2 Release Dry-run Pages Evidence Report

## Scope

- Release dry-run validation
- GitHub Pages workflow evidence
- Visual baseline refresh

## Changes

- Refreshed Playwright visual baselines after the insurance UI expansion increased dashboard height.
- Confirmed visual regression passes against the refreshed desktop and mobile screenshots.
- Confirmed GitHub Pages workflow contract uses `validate:feature` for pull requests, `validate:full` for `master`, `validate:release` for manual release, and uploads `frontend` as the Pages artifact.

## Validation

- `npm run visual:screenshots` passed.
- `npm run validate:visual-regression` passed.
- `npm run validate:deployment` passed.
- `npm run validate:preview-smoke` passed.
- `npm run validate:release:dry-run` passed with 103/103 steps.

## GitHub Pages Evidence

- Workflow file: `.github/workflows/pages.yml`.
- Build artifact path: `frontend`.
- Local deployment contract: `npm run validate:deployment` passed with 14 artifacts.
- Local preview path smoke: `npm run validate:preview-smoke` passed with 7 project-path assets.
- Remote workflow status was not queried because `gh` CLI is not installed in this environment.
